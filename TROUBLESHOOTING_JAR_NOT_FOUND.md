# ❌ Ошибка: Unable to access jarfile ProxyPass.jar

## 🔍 Диагностика проблемы

### Шаг 1: Проверь что сборка прошла успешно

В конце вывода `gradlew.bat shadowJar` должно быть:
```
BUILD SUCCESSFUL in 2m 34s
```

Если НЕ было `BUILD SUCCESSFUL` - значит сборка ПРОВАЛИЛАСЬ!

**Решение:** Скопируй ПОСЛЕДНИЕ 20 строк вывода и покажи мне ошибку.

---

### Шаг 2: Найди где лежит JAR файл

**Запусти эту команду:**
```cmd
dir /s ProxyPass.jar
```

Это найдет ВСЕ файлы ProxyPass.jar на диске.

Или проверь конкретно:
```cmd
dir build\libs\ProxyPass.jar
```

**Если показывает "Файл не найден"** → файл НЕ был создан!

---

### Шаг 3: Проверь где ты находишься

```cmd
cd
```

Должно показать что-то типа:
```
C:\Users\mihaz\Downloads\23\...\ProxyPass-master\ProxyPass-master
```

**Если НЕТ** - перейди:
```cmd
cd ProxyPass-master\ProxyPass-master
```

---

## ✅ Правильная последовательность

**Полная инструкция от А до Я:**

```cmd
REM 1. Перейди в папку проекта
cd C:\Users\mihaz\Downloads\23\new-claude-session-setup-011CUrfdM6oHdxQbJFQaYQYJ\new-claude-session-setup-011CUrfdM6oHdxQbJFQaYQYJ\ProxyPass-master\ProxyPass-master

REM 2. Проверь что ты в правильном месте
dir build.gradle.kts

REM 3. Собери проект (ДОЖДИСЬ окончания!)
gradlew.bat clean shadowJar

REM 4. Проверь что файл создан
dir build\libs\ProxyPass.jar

REM 5. Запусти (указывай ПОЛНЫЙ путь!)
java -jar build\libs\ProxyPass.jar
```

---

## 💡 Типичные ошибки

### Ошибка 1: Сборка не завершилась
**Симптом:** Не было `BUILD SUCCESSFUL`

**Решение:** Скопируй последние строки с ошибкой, я исправлю код.

---

### Ошибка 2: Файл не создан
**Симптом:** `dir build\libs\ProxyPass.jar` → "Файл не найден"

**Решение:**
```cmd
REM Очисти и собери заново
gradlew.bat clean
gradlew.bat shadowJar
```

---

### Ошибка 3: Неправильный путь при запуске
**Симптом:** "Unable to access jarfile ProxyPass.jar"

**НЕПРАВИЛЬНО:**
```cmd
java -jar ProxyPass.jar  ❌
```

**ПРАВИЛЬНО:**
```cmd
java -jar build\libs\ProxyPass.jar  ✅
```

Или с полным путем:
```cmd
java -jar C:\Users\mihaz\Downloads\23\...\ProxyPass-master\ProxyPass-master\build\libs\ProxyPass.jar
```

---

### Ошибка 4: Не та папка
**Симптом:** Файл есть, но не находит

**Проверь:**
```cmd
REM Где я?
cd

REM Где файл?
dir /s ProxyPass.jar
```

**Решение:** Перейди в папку где ТОЧНО лежит build.gradle.kts:
```cmd
cd ProxyPass-master\ProxyPass-master
java -jar build\libs\ProxyPass.jar
```

---

## 🎯 Быстрая диагностика (копируй и запускай)

**Скопируй ЭТО и покажи мне результат:**

```cmd
echo === Где я нахожусь? ===
cd

echo.
echo === Есть ли build.gradle.kts? ===
dir build.gradle.kts

echo.
echo === Есть ли JAR файл? ===
dir build\libs\ProxyPass.jar

echo.
echo === Поиск JAR по всему проекту ===
dir /s ProxyPass.jar
```

Скопируй ВЕСЬ вывод этих команд - я точно скажу что делать!

---

## 🚀 Альтернативный способ - запуск через gradlew

Вместо `java -jar` можешь запустить так:

```cmd
gradlew.bat run
```

Это автоматически соберет и запустит проект!

---

## 📂 Проверка структуры файлов

**Правильная структура:**
```
ProxyPass-master\ProxyPass-master\
├── build\                    ← Создается после сборки
│   └── libs\
│       └── ProxyPass.jar     ← ЭТОТ файл нужен!
├── src\
├── build.gradle.kts          ← Должен быть здесь
└── gradlew.bat               ← Должен быть здесь
```

**Проверь что build.gradle.kts и gradlew.bat в ОДНОЙ папке с build\**

---

## 📞 Что делать дальше?

**Вариант А:** Если сборка ПРОШЛА (было `BUILD SUCCESSFUL`):
```cmd
cd ProxyPass-master\ProxyPass-master
java -jar build\libs\ProxyPass.jar
```

**Вариант Б:** Если сборка УПАЛА (были ошибки):
→ Покажи мне последние 20 строк ошибки

**Вариант В:** Если вообще непонятно что происходит:
→ Выполни команды из раздела "Быстрая диагностика" выше и покажи результат

---

**Жду твой ответ!** 🛠️
