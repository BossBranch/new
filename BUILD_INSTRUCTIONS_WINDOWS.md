# 🔨 Инструкция по сборке ProxyPass на Windows

## Требования

- **Windows 10/11**
- **Java 17+** ([скачать](https://adoptium.net/temurin/releases/))
- **Git** (опционально, если клонировать через GitHub)

## Шаг 1: Установка Java

1. Скачай **Java 17** или выше: https://adoptium.net/temurin/releases/
2. Выбери **Windows x64** → **JDK** → **Installer (.msi)**
3. Установи Java
4. Проверь установку:
   ```cmd
   java -version
   ```
   Должно показать версию 17 или выше

## Шаг 2: Скачать проект

**Вариант А: Через Git**
```cmd
git clone https://github.com/BossBranch/new.git
cd new
git checkout claude/session-setup-011CUrfdM6oHdxQbJFQaYQYJ
```

**Вариант Б: Скачать ZIP**
1. Зайди на: https://github.com/BossBranch/new
2. Выбери ветку `claude/session-setup-011CUrfdM6oHdxQbJFQaYQYJ`
3. Нажми **Code** → **Download ZIP**
4. Распакуй архив

## Шаг 3: Сборка проекта

1. Открой **PowerShell** или **CMD**
2. Перейди в папку:
   ```cmd
   cd ProxyPass-master\ProxyPass-master
   ```

3. **Собери проект:**
   ```cmd
   gradlew.bat shadowJar
   ```

   Или просто:
   ```cmd
   gradlew shadowJar
   ```

4. Дождись завершения сборки (может занять 1-3 минуты при первой сборке)

## Шаг 4: Результат

После успешной сборки найдешь JAR файл:
```
build\libs\ProxyPass.jar
```

Размер файла: примерно **15-20 MB**

## Шаг 5: Настройка

Создай файл `config.yml` в папке с JAR:

```yaml
## Address proxy will bind to.
proxy:
  host: 0.0.0.0
  port: 19132

## Destination server (Craft.PE)
destination:
  host: Craft.PE
  port: 19132

## Maximum clients (0 = unlimited)
max-clients: 0

## Encode and decode packets for debugging
packet-testing: false

## Log packets for each session
log-packets: true

## Where to log: console, file or both
log-to: file

## Packets to ignore (reduce spam)
ignored-packets:
  - "NetworkStackLatencyPacket"
  - "LevelChunkPacket"
  - "MovePlayerPacket"
  - "PlayerAuthInputPacket"
  - "NetworkChunkPublisherUpdatePacket"
  - "ClientCacheBlobStatusPacket"
  - "ClientCacheMissResponsePacket"
```

## Шаг 6: Запуск

```cmd
java -jar ProxyPass.jar
```

Должно показать:
```
[INFO] Loading configuration...
[INFO] Loading server...
[INFO] Bedrock server 1.21.111 (844) started on 0.0.0.0:19132
```

## Шаг 7: Подключение

В Minecraft Bedrock:
- **IP**: `127.0.0.1` (если на том же ПК)
- **Порт**: `19132`

Или с другого ПК:
- **IP**: IP адрес твоего компьютера
- **Порт**: `19132`

## 🎮 Как работает Hit Detector

Когда тебя бьют, в чате появится:
```
[HIT] PlayerName hit you from 3.45 blocks
```

**Формат сообщения:**
- `[HIT]` - префикс (красный)
- `PlayerName` - ник атакующего
- `3.45 blocks` - расстояние в блоках

**Типичные расстояния:**
- **3-4 блока** - нормально
- **5-6 блоков** - подозрительно
- **7+ блоков** - скорее всего чит (reach hack)

## ❌ Возможные ошибки

### 1. `'java' is not recognized`
**Решение:** Java не установлена или не добавлена в PATH
- Переустанови Java
- Или укажи полный путь: `"C:\Program Files\Eclipse Adoptium\jdk-17\bin\java.exe" -jar ProxyPass.jar`

### 2. `gradlew: command not found`
**Решение:** Используй `gradlew.bat` вместо `gradlew`:
```cmd
gradlew.bat shadowJar
```

### 3. `Connection refused` при подключении
**Решение:**
- Проверь что прокси запущен
- Проверь firewall Windows
- Попробуй выключить антивирус

### 4. `Address already in use`
**Решение:** Порт 19132 занят другим процессом
- Закрой другие Minecraft серверы/прокси
- Или измени порт в `config.yml` на 19133

### 5. Сборка зависает на "Downloading..."
**Решение:** Проблемы с интернетом
- Проверь подключение
- Отключи VPN/прокси
- Попробуй снова

## 📂 Структура файлов

```
ProxyPass-master/
└── ProxyPass-master/
    ├── build/
    │   └── libs/
    │       └── ProxyPass.jar      ← Готовый файл
    ├── src/
    │   └── main/
    │       ├── java/              ← Исходный код
    │       └── resources/
    │           └── config.yml     ← Конфиг (шаблон)
    ├── build.gradle.kts
    ├── gradlew.bat               ← Используй это для сборки
    └── gradlew
```

## 🚀 Быстрый старт (все команды)

```cmd
REM 1. Перейти в папку
cd ProxyPass-master\ProxyPass-master

REM 2. Собрать
gradlew.bat shadowJar

REM 3. Запустить
java -jar build\libs\ProxyPass.jar
```

## 📖 Дополнительно

**Логи сохраняются в:**
- `sessions/` - логи каждой игровой сессии
- `data/` - дампы пакетов и данных

**Отладка:**
```cmd
REM Запуск с debug логами
java -jar ProxyPass.jar --debug

REM Проверить что прокси слушает порт
netstat -an | findstr 19132
```

## ✅ Проверка установки

1. **Java установлена:**
   ```cmd
   java -version
   ```
   ✅ Показывает версию 17+

2. **Проект собран:**
   ```cmd
   dir build\libs\ProxyPass.jar
   ```
   ✅ Файл существует

3. **Прокси запущен:**
   ```cmd
   java -jar build\libs\ProxyPass.jar
   ```
   ✅ Показывает "Bedrock server started on..."

4. **Клиент подключился:**
   - В логах прокси: `[INFO] Player connected`
   - В игре получаешь сообщения о ударах

## 🆘 Помощь

Если что-то не работает:
1. Проверь версию Java: `java -version` (должна быть 17+)
2. Проверь что ты в правильной папке: `dir gradlew.bat`
3. Посмотри логи в консоли
4. Проверь `sessions/` папку для детальных логов

---

**Версия:** 1.0
**Поддержка:** Minecraft Bedrock 1.21.111 (Protocol v844)
**Автор модификации:** Hit Detector для античита
