# 🔧 Быстрое решение проблем со сборкой на Windows

## ❌ Проблема: "gradlew не найден" или "не является командой"

### ✅ Решение 1: Используй .bat версию

**НЕПРАВИЛЬНО** (Linux команда):
```cmd
gradlew shadowJar
```

**ПРАВИЛЬНО** (Windows команда):
```cmd
gradlew.bat shadowJar
```

Или просто:
```cmd
.\gradlew shadowJar
```

---

## ❌ Проблема: "Не в той папке"

### ✅ Решение 2: Проверь путь

**Шаг 1: Проверь где ты находишься**
```cmd
dir
```

Должны видеть файлы:
- `gradlew.bat` ✅
- `build.gradle.kts` ✅
- `settings.gradle.kts` ✅

**Шаг 2: Если файлов НЕТ - перейди в правильную папку:**
```cmd
cd ProxyPass-master\ProxyPass-master
```

**Шаг 3: Проверь еще раз:**
```cmd
dir gradlew.bat
```

Должно показать файл. Если показывает - ты в правильном месте!

---

## 🎯 Правильная последовательность команд

**Весь процесс от А до Я:**

```cmd
REM 1. Перейди в корень проекта (где распаковал архив)
cd C:\Users\ТвойЮзер\Downloads\new-main

REM 2. Проверь структуру
dir

REM 3. Перейди в папку ProxyPass
cd ProxyPass-master\ProxyPass-master

REM 4. Проверь что gradlew.bat есть
dir gradlew.bat

REM 5. Собери проект
gradlew.bat shadowJar

REM 6. Если шаг 5 не работает, попробуй так:
.\gradlew shadowJar

REM 7. Дождись окончания сборки (может занять 2-5 минут)

REM 8. Проверь что JAR создан
dir build\libs\ProxyPass.jar

REM 9. Запусти
java -jar build\libs\ProxyPass.jar
```

---

## 📂 Правильная структура папок

Вот как должно выглядеть:

```
📁 new-main/                          ← Корень (скачанный архив)
  ├── 📁 Protocol-3.0/
  ├── 📁 ProxyPass-master/
  │    └── 📁 ProxyPass-master/       ← СЮДА заходить!
  │         ├── 📄 gradlew.bat        ← Этот файл должен быть
  │         ├── 📄 build.gradle.kts
  │         ├── 📄 settings.gradle.kts
  │         ├── 📁 gradle/
  │         ├── 📁 src/
  │         └── 📁 build/             ← Появится после сборки
  │              └── 📁 libs/
  │                   └── 📄 ProxyPass.jar  ← Готовый файл
  ├── 📄 HITDETECTOR_README.md
  └── 📄 BUILD_INSTRUCTIONS_WINDOWS.md
```

**ВАЖНО:** Ты должен быть в папке `ProxyPass-master\ProxyPass-master` (там где `gradlew.bat`)

---

## 🔍 Диагностика проблемы

**Проверка #1: Где я нахожусь?**
```cmd
cd
```
Ответ должен быть типа: `C:\...\ProxyPass-master\ProxyPass-master`

**Проверка #2: Есть ли gradlew.bat?**
```cmd
dir gradlew.bat
```
Ответ: должен показать файл ~2-3 KB

**Проверка #3: Есть ли Java?**
```cmd
java -version
```
Ответ: должен показать версию 17 или выше

**Проверка #4: Есть ли build.gradle.kts?**
```cmd
dir build.gradle.kts
```
Ответ: должен показать файл ~1 KB

Если **ВСЕ 4 проверки** прошли ✅ → можешь собирать!

---

## 💡 Типичные ошибки

### Ошибка 1: `'gradlew' is not recognized`
**Причина:** Забыл `.bat` или `.\` в начале

**Решение:**
```cmd
REM Вместо:
gradlew shadowJar

REM Используй:
gradlew.bat shadowJar
```

### Ошибка 2: `Could not find or load main class org.gradle.wrapper.GradleWrapperMain`
**Причина:** Не в той папке

**Решение:**
```cmd
cd ProxyPass-master\ProxyPass-master
gradlew.bat shadowJar
```

### Ошибка 3: `'java' is not recognized`
**Причина:** Java не установлена

**Решение:**
1. Скачай Java 17+: https://adoptium.net/temurin/releases/
2. Установи
3. Перезапусти CMD/PowerShell
4. Попробуй снова

### Ошибка 4: `Task 'shadowJar' not found`
**Причина:** Не в той папке или поврежден `build.gradle.kts`

**Решение:**
1. Проверь что ты в `ProxyPass-master\ProxyPass-master`
2. Проверь что файл `build.gradle.kts` существует
3. Перескачай проект если нужно

---

## 🎮 После успешной сборки

**Ты увидишь:**
```
BUILD SUCCESSFUL in 2m 34s
3 actionable tasks: 3 executed
```

**Готовый файл:**
```
build\libs\ProxyPass.jar
```

**Запуск:**
```cmd
java -jar build\libs\ProxyPass.jar
```

**Успешный запуск выглядит так:**
```
[INFO] Loading configuration...
[INFO] Loading server...
[INFO] Bedrock server 1.21.111 (844) started on 0.0.0.0:19132
```

---

## 📞 Если ничего не помогло

**Отправь мне вывод этих команд:**

```cmd
REM 1. Где я нахожусь?
cd

REM 2. Что в папке?
dir

REM 3. Есть ли Java?
java -version

REM 4. Полный путь к gradlew.bat
dir /s gradlew.bat
```

Скопируй весь вывод - разберемся!

---

## ⚡ Супер-быстрый способ (копируй и вставляй)

**Скопируй ВСЕ эти команды сразу в CMD:**

```cmd
cd ProxyPass-master\ProxyPass-master && dir gradlew.bat && gradlew.bat shadowJar && dir build\libs\ProxyPass.jar && echo Готово! Запускай: java -jar build\libs\ProxyPass.jar
```

Если работает - увидишь `Готово!` и инструкцию по запуску.

---

**Удачи! 🚀**
